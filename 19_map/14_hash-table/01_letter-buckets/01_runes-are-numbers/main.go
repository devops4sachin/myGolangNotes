package main

import "fmt"

func main() {
	letter := 'A' // runes are numbers, Runes are always written in 'single quotes', "double" and `back ticks` are used for Strings.
	fmt.Println(letter)
	fmt.Printf("%T \n", letter)
}
